import Foundation

/// 용어 정리
/// Node : 연결 리스트처럼, 트리 또한 Node들로 구성된다. 노드는 값과 자식 노드들을 갖는다.
/// 부모와 자식 : 최상부의 노드를 Root Node 라고 한다.
///         Root Node 를 제외한 모든 노드들은 위에 한 개의 노드를 갖는데, 이를 Parent Node 라고 한다.
///         이와 반대로, Child Node 들은 아래에 위치한다.
/// Leaf : Child Node 가 없는 Node 를 뜻한다.
